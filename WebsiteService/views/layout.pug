doctype html
html
  head
    title #{title}
    link(rel='stylesheet', href='/styles/global.css')
    script(src='/scripts/global.js')
    block head
  body
    .main-container
      .left-sidebar
        ul
          li
            a(href='/home') Home
          li
            a(href='/explore') Explore
          li
            a(href='/notifications') Notifications
          li
            a(href='/messages') Messages
          li
            a(href='/bookmarks') Bookmarks
          li
            a(href=`/profile/${user.Username}`) Profile
        button(type='button', id='postButton') Post
        
        .profile
          img(src=`https://capstonemedia.s3.amazonaws.com/${user.Profile.profileImage}` alt='Profile Picture')
          .profile-info
            if user && user.Profile
              strong #{user.Profile.name}
              span @#{user.Username}
          ul.profile-dropdown
            li
              a(href='/settings') Settings
            li
              a(href='/logout') Logout

      .content
        block content

      .right-sidebar
        .trending
          h3 What’s happening
          ul
            li Trending 1
            li Trending 2
            li Trending 3
        .who-to-follow
          h3 Who to follow
          ul
            li User 1
            li User 2
            li User 3

    // Modal
    .modal#postModal
      .modal-content
        span.close-button &times;
        .modal-header
          img.profile-picture(src=`https://capstonemedia.s3.amazonaws.com/${user.Profile.profileImage}` alt='Profile Picture')
          textarea(placeholder='What is happening?!') 
        .modal-footer
          .actions
            button(type='button', id='submitPost') Post
            label.image-upload
              input(type='file', accept='image/*', id='imageInput', style='display: none;')
              i.icon-image  

